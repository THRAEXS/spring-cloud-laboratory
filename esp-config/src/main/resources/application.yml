spring:
  application:
    name: esp-config
  cloud:
    config:
      server:
        git:
          uri: http://10.27.213.70/esp/esp-cloud-config.git
          searchPaths: '{application}'
          username: '{cipher}3795668e0b759504df110ea0433beccd4b942f481ac9ba7f9466e7b3f4991a9e'
          password: '{cipher}d36f3c490a710cb3a7cf5082849c9f74fb153f9ba164b12bada070e120b6653d'

server:
  port: ${CFG_CONFIG_PORT:8750}

eureka:
  instance:
    instanceId: ${spring.application.name}:${spring.cloud.client.hostname}:${server.port}
  client:
    serviceUrl:
      defaultZone: http://${CFG_EUREKA_HOST:${spring.cloud.client.hostname}}:${CFG_EUREKA_PORT:8761}/eureka/